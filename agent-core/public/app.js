const messages=document.querySelector('#messages');const tools=document.querySelector('#tools');const input=document.querySelector('#input');const ws=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}`);let currentAssistant=null;const toolCards=new Map();ws.onmessage=(e)=>{const ev=JSON.parse(e.data);if(ev.type==='turn_start'){currentAssistant=el('div','msg');currentAssistant.innerHTML='<b>assistant</b><div class="delta"></div>';messages.append(currentAssistant)}if(ev.type==='message_delta'&&ev.delta.type==='content_delta'){(currentAssistant?.querySelector('.delta')||messages).textContent+=ev.delta.delta}if(ev.type==='tool_execution_start'){const card=el('div','tool');card.innerHTML=`<b>${ev.toolName}</b><pre>${JSON.stringify(ev.args,null,2)}</pre><small>running</small>`;toolCards.set(ev.toolCallId,card);tools.prepend(card)}if(ev.type==='tool_execution_end'){const card=toolCards.get(ev.toolCallId);if(card){if(ev.isError)card.classList.add('error');card.querySelector('small').textContent=ev.isError?`error: ${ev.error}`:'done';const pre=el('pre');pre.textContent=JSON.stringify(ev.result??ev.error,null,2).slice(0,1200);card.append(pre)}}};document.querySelector('#form').onsubmit=(e)=>{e.preventDefault();const text=input.value.trim();if(!text)return;const m=el('div','msg');m.innerHTML=`<b>user</b><div>${escapeHtml(text)}</div>`;messages.append(m);ws.send(JSON.stringify({type:text.startsWith('/tree ')?'tree':'message',text}));input.value=''};function el(t,c){const n=document.createElement(t);if(c)n.className=c;return n}function escapeHtml(s){return s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
