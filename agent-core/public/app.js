const messages=document.querySelector('#messages');const tools=document.querySelector('#tools');const input=document.querySelector('#input');const statusEl=document.querySelector('#ws-status');const metricBody=document.querySelector('#metrics tbody');const canvas=document.querySelector('#equity');let ws,currentAssistant,reconnectTimer;const toolCards=new Map();connect();function connect(){statusEl.textContent='connecting';ws=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}`);ws.onopen=()=>{statusEl.textContent='connected'};ws.onclose=()=>{statusEl.textContent='disconnected; reconnecting';clearTimeout(reconnectTimer);reconnectTimer=setTimeout(connect,1000)};ws.onerror=()=>ws.close();ws.onmessage=(e)=>handleEvent(JSON.parse(e.data))}function handleEvent(ev){if(ev.type==='turn_start'){currentAssistant=el('div','msg');currentAssistant.innerHTML='<b>assistant</b><div class="delta"></div>';messages.append(currentAssistant)}if(ev.type==='message_delta'&&ev.delta.type==='content_delta'){(currentAssistant?.querySelector('.delta')||messages).textContent+=ev.delta.delta}if(ev.type==='tool_execution_start'){const card=el('div','tool');card.innerHTML=`<b>${ev.toolName}</b><pre>${JSON.stringify(ev.args,null,2)}</pre><small>running</small>`;toolCards.set(ev.toolCallId,card);tools.prepend(card)}if(ev.type==='tool_execution_end'){const card=toolCards.get(ev.toolCallId);if(card){if(ev.isError)card.classList.add('error');card.querySelector('small').textContent=ev.isError?`error: ${ev.error}`:'done';const pre=el('pre');pre.textContent=JSON.stringify(ev.result??ev.error,null,2).slice(0,1200);card.append(pre)}const details=ev.result?.details;if(details?.equity_curve&&details?.metrics)renderBacktest(details)}}document.querySelector('#form').onsubmit=(e)=>{e.preventDefault();const text=input.value.trim();if(!text)return;const m=el('div','msg');m.innerHTML=`<b>user</b><div>${escapeHtml(text)}</div>`;messages.append(m);if(ws.readyState===WebSocket.OPEN)ws.send(JSON.stringify({type:text.startsWith('/tree ')?'tree':'message',text}));input.value=''};function renderBacktest(result){drawEquity(result.equity_curve);const labels={sharpe:'夏普',max_drawdown:'最大回撤',win_rate:'胜率',annual_return:'年化收益'};metricBody.innerHTML='';for(const [k,label] of Object.entries(labels)){const tr=el('tr');tr.innerHTML=`<td>${label}</td><td>${formatMetric(k,result.metrics[k])}</td>`;metricBody.append(tr)}}function drawEquity(points){const ctx=canvas.getContext('2d'),w=canvas.width,h=canvas.height;ctx.clearRect(0,0,w,h);if(!points.length)return;const vals=points.map(p=>p.equity),min=Math.min(...vals),max=Math.max(...vals),pad=24;ctx.strokeStyle='#334155';ctx.beginPath();ctx.moveTo(pad,pad);ctx.lineTo(pad,h-pad);ctx.lineTo(w-pad,h-pad);ctx.stroke();ctx.strokeStyle='#38bdf8';ctx.lineWidth=2;ctx.beginPath();points.forEach((p,i)=>{const x=pad+i*(w-2*pad)/Math.max(points.length-1,1);const y=h-pad-(p.equity-min)/Math.max(max-min,1)*(h-2*pad);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()}function formatMetric(k,v){if(v==null)return '-';return ['max_drawdown','win_rate','annual_return'].includes(k)?(v*100).toFixed(2)+'%':Number(v).toFixed(3)}function el(t,c){const n=document.createElement(t);if(c)n.className=c;return n}function escapeHtml(s){return s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
